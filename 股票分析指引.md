# 股票分析指引

> 当用户输入股票代码（如 `002279`、`600519`）要求分析走势时，按本指引执行。

## 触发条件

用户消息中出现以下任一情况，即触发本工作流：
- 直接输入 6 位 A 股代码（如 "002279"、"分析下 600519"）
- 明确要求分析某只股票的本周/近期/某段时间走势
- 提及"威科夫分析"且指定具体股票

## 工作流：5 步闭环

### 第一步：加载技能

调用两个 skill（若尚未加载）：
- `a-share-kline-fetch`：数据获取 + 简报管理
- `wyckoff-trading`：威科夫分析框架

### 第二步：准备环境（首次或环境变更时）

全项目统一使用仓库根目录 `.venv`（uv 管理），各 skill / 子项目不单独建环境。

```bash
# 首次：用 uv 在根目录创建统一 venv 并安装依赖
uv venv .venv
uv pip install --python .venv/bin/python pandas requests

# 探测统一 venv 并把 python 路径写入 skill 目录 .python_path
bash .agents/skills/a-share-kline-fetch/setup_env.sh
```

成功后在 skill 目录生成 `.python_path` 文件。后续命令复用此路径：

```bash
SKILL_DIR=.agents/skills/a-share-kline-fetch
PY=$(cat $SKILL_DIR/.python_path 2>/dev/null || echo python3)
```

### 第三步：拉取数据 + 提取股票名称

```bash
$PY $SKILL_DIR/fetch_kline.py <code> [背景月数] [本周一日期YYYY-MM-DD]
```

**参数**：
- `code`：6 位股票代码（必填）
- `背景月数`：默认 6，建议取 6 以充分识别长期背景
- `本周一日期`：默认自动识别本周一；回测历史时指定

**输出**：
- 第一行：`股票: <code>  <名称>  本周: ...  背景: ...`
- 第二行：`STOCK_NAME=<名称>`（agent 用此值传入下一步）
- 4 个数据块：本周日线 / 背景数据 / 统计分析 / 最近30日明细

**示例**：
```bash
$PY $SKILL_DIR/fetch_kline.py 002279 6 2026-07-27
```

### 第四步：准备简报（归档历史 + 生成新模板）

从第三步输出中提取 `STOCK_NAME=` 后的名称，传入 workflow.py：

```bash
$PY $SKILL_DIR/workflow.py <code> [分析日期YYYY-MM-DD] "<股票名称>"
```

**参数**：
- `code`：6 位股票代码（必填）
- `分析日期`：默认今天，格式 `YYYY-MM-DD`
- `股票名称`：可选，传入则简报标题带名称（推荐传入，可读性更强）

**输出**：
- `NEW_BRIEF=<新简报绝对路径>`：agent 把分析写入此文件
- `ARCHIVED=<归档路径|NONE>`：若有归档，说明基于历史迭代
- `STOCK_NAME=<名称>`：透传的名称
- `VERSION=v<n>`：版本号
- `HISTORY_BEGIN ... HISTORY_END`：旧简报全文（首次为"无历史简报"）

**机制**：
- 首次分析：生成 v1 简报模板
- 同股同月再次分析：旧简报自动归档到 `archive/<code>/<code>_<date>_v1.md`，新模板为 v2 并引用历史

**示例**：
```bash
# 从 fetch_kline 输出提取名称
NAME=$($PY $SKILL_DIR/fetch_kline.py 002279 6 2026-07-27 | grep "^STOCK_NAME=" | cut -d= -f2)
$PY $SKILL_DIR/workflow.py 002279 2026-07-31 "$NAME"
```

### 第五步：写入分析 + 更新知识库

#### 5.1 写入分析内容

把第三步的数据 + `wyckoff-trading` skill 的"看盘五步法"分析，填入 `NEW_BRIEF` 指向的模板文件。

**必须填写的章节**：
- 〇 历史上下文（若有归档，对照上次预判）
- 一 数据摘要（从 fetch_kline 输出抄入）
- 二 看盘五步法（背景→形态→性质→结论→行动）
- **三 本次预判**（方向/关键价位/时间窗口/触发条件）— 用于下次迭代验证
- 五 风险提示

#### 5.2 更新迭代知识库

打开 `analysis-brief/KNOWLEDGE.md`，按 4 个维度检查并更新：

| 维度 | 何时更新 | 更新方式 |
|---|---|---|
| 模式总结 | 识别到跨个股共性形态 | 在表格末尾追加条目 |
| 个股跨期迭代 | 同股有历史简报时 | 对照上次预判 vs 实际走势，记录偏差 |
| 规则修正 | 发现 A 股本地化调整 | 记录原规则 + 修正 + 理由 |
| 失败案例库 | 预判被市场证伪 | 不掩饰，记录根因 + 防范措施 |

**重要**：每次分析后必须检查这 4 个维度，有新发现则更新，无则保持不变。

## 简报目录结构

```
analysis-brief/
├── KNOWLEDGE.md              # 顶层迭代知识文档（4维度）
├── archive/                  # 历史归档（可追溯）
│   └── <code>/
│       └── <code>_<date>_v<n>.md
└── <YYYY-MM>/                # 按年月分目录（当月最新简报）
    └── <code>_<date>.md      # 文件名格式：<code>_<分析日期>.md
```

## 看盘五步法框架（来自 wyckoff-trading skill）

1. **背景判断**：牛/熊/震荡区；处于吸筹/派发的哪个阶段
2. **价量形态**：识别 SC/AR/ST/SOS/JOC/UT/SOW 等关键 K 线
3. **形态性质**：努力-结果关系；停止行为；吸收行为
4. **结论/预测**：大背景 + 短期 + 关键价位 + 当前阶段
5. **措施和行动**：进场/放弃/跟进的条件与行动

## 完整示例：分析 002279

```bash
# 1. 准备环境（统一 venv，uv 管理；缺失时 setup_env.sh 会用 uv 自动创建）
bash .agents/skills/a-share-kline-fetch/setup_env.sh
SKILL_DIR=.agents/skills/a-share-kline-fetch
PY=$(cat $SKILL_DIR/.python_path)

# 2. 拉取数据（输出含 STOCK_NAME=久其软件）
$PY $SKILL_DIR/fetch_kline.py 002279 6 2026-07-27

# 3. 准备简报（传入名称）
$PY $SKILL_DIR/workflow.py 002279 2026-07-31 "久其软件"

# 4. agent 把分析内容写入 NEW_BRIEF 指向的文件
#    路径示例：analysis-brief/2026-07/002279_2026-07-31.md

# 5. agent 更新 analysis-brief/KNOWLEDGE.md
```

## 故障排查

| 现象 | 原因 | 解决 |
|---|---|---|
| `STOCK_NAME=` 为空 | 腾讯接口未返回 qt 字段 | workflow.py 可不传名称，标题只显示代码 |
| `.python_path` 不存在 | 未运行 setup_env.sh | 先运行 `bash setup_env.sh` |
| `ModuleNotFoundError: pandas` | 根目录统一 venv 缺少 pandas/requests | 根目录运行 `uv pip install --python .venv/bin/python pandas requests` |
| 拉取数据少于预期 | 腾讯单次最多 640 条 | 缩短背景月数 |
| `[ERROR] 无数据返回` | 代码错误或接口限流 | 检查代码前缀；稍后重试 |

## 适用范围

- 仅 A 股（沪深），不含港股/美股/期货
- 仅日线，不含分钟线
- 不做交易决策——分析结论仅供参考，决策由使用者自负
